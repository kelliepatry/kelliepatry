# Input validation review — 2026-09-17

The original evaluator admitted null authority at a human gate when authorization_bound=true and null target status at a terminal edge. It is read-only; these findings do not show a persisted unauthorized transition.

The corrected evaluator rejects null authority and missing/blank phases and statuses. All 99,144 original combinations retain zero mismatches; 13 negative-input checks pass, including a rerun against the deployed evaluator.

The original controlled proposal binds obsolete hashes and must not be executed. The corrected graph and unchanged 31-row seed were subsequently deployed under a fresh exact-hash proposal and explicit human authorization. The existing specification transition writer was not cut over.

The studio authorization integration issue was repaired and tested: an attributable human studio governance decision can authorize class-C studio changes while exact-target/hash/expiry and role checks remain enforced. No project decision was fabricated.

Canonical applied migration records and implementation evidence are maintained in the private studio-home repository. This folder remains the review/proposal source; original README hashes and proposal-only migration comments describe its historical predeployment packet.
