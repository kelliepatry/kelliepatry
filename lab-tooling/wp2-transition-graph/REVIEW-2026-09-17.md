# Input validation review — 2026-09-17

The prior proposal's 99,144-case parity test covered valid, non-null state and authority values. Additional tests demonstrated that a null authority could pass a human gate when authorization_bound=true and a null target status could pass the terminal edge. The evaluator is read-only; these findings are not evidence of a persisted unauthorized transition.

The corrected candidate rejects null authority and missing/blank from/to phases and statuses. All 99,144 original combinations retain zero mismatches; 13 negative-input checks pass. Tests ran against temporary objects inside a rolled-back transaction. No production DDL or seed was applied.

The existing CCS c9d7dec9-4dc8-4d63-8539-2a571f12218d binds the old file hashes and must not be used for this corrected candidate. Re-propose using the corrected file hashes and current database structure hash. The original README's hashes describe the original proposal only. The seed is unchanged.

An additional integration issue requires resolution before authorization: the studio governance authorization helper writes gov_decision_id with decision_id=NULL, while authorize_controlled_change_set_v1 reads attributable decided_by only through v_current_decisions by decision_id. Do not fabricate a project decision or bypass this check. Confirm the supported studio authorization route before token issuance.
